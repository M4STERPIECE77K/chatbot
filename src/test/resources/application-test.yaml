spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:15:///chatbot_db
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

gemini:
  api:
    key: test-key