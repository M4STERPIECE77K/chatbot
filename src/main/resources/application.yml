spring:
  application:
    name: chatbot-backend

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5400/chatbot_db}
    username: ${SPRING_DATASOURCE_USERNAME:chatbot_user}
    password: ${SPRING_DATASOURCE_PASSWORD:chatbot_pass}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0

server:
  port: 8000

gemini:
  api:
    key: AIzaSyBm1TE9Yaum5F-kP4h92JZTpAaYfnHK6Ss
api:
  version: ${API_VERSION:v1}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized